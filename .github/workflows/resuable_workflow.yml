name: Resuable Workflow
on:
  workflow_call:
    inputs:
        name:
            description: Enter the name
            required: true
            type: string

jobs:
    reusable_workflow:
        runs-on: ubuntu-latest
        steps:
            - name: Resuable Workflow
              run: echo "Welcome to ${{inputs.name}} the reusable workflow" 
            

